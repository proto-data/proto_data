packages:
  base:
  interface:
  data:
  protobuf:
  codec:
dependencies:

descriptions:
  base:
  interface:
  data: data model implementation with builder
  protobuf: protobuf interface
  codec: Converters for data from/to protobuf
